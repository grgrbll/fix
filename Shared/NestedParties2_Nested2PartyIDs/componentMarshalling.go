package NestedParties2_Nested2PartyIDs

import "bufio"
import "strconv"
import "fmt"

// Autogenerated at , do not edit



import NstdPtys2SubGrp_Nested2PartySubIDs "grgrbll/fix/Shared/NstdPtys2SubGrp_Nested2PartySubIDs"


func (m *NestedParties2_Nested2PartyIDs) MarshalFIX(res *bufio.Writer) error {
    var err error
    
    if m.Nested2PartyID.HasValue() {
        (*res).WriteString("757=")
        val, err := m.Nested2PartyID.MarshalFIX()
        if err != nil {
            return err
        }
        (*res).Write(val)
        (*res).WriteString("\x01")
    }
    if m.Nested2PartyIDSource.HasValue() {
        (*res).WriteString("758=")
        val, err := m.Nested2PartyIDSource.MarshalFIX()
        if err != nil {
            return err
        }
        (*res).Write(val)
        (*res).WriteString("\x01")
    }
    if m.Nested2PartyRole.HasValue() {
        (*res).WriteString("759=")
        val, err := m.Nested2PartyRole.MarshalFIX()
        if err != nil {
            return err
        }
        (*res).Write(val)
        (*res).WriteString("\x01")
    }
    if len(m.Nested2PartySubIDs) > 0 {
    
    (*res).WriteString("806=")
    (*res).WriteString(strconv.Itoa(len(m.Nested2PartySubIDs)))
    (*res).WriteString("\x01")
    for _, g := range m.Nested2PartySubIDs {
        err = g.MarshalFIX(res)
    }
    }
    
    return err
}

func (m *NestedParties2_Nested2PartyIDs) UnmarshalFIX(input io.Reader) error {
    var err error
    var field []byte
    for field, err = input.ReadSlice([]byte("\x01")); err == nil {
        slices = bytes.Split(field, []byte("="))
        if len(slices) != 2 {
            // TODO handle data fields
            err = errors.New(fmt.Sprintf("Found field without seperator '=' (%s)", string(field))
        } else {
            var used bool
            used, err m.UnmarshalFieldFIX(strconv.Atoi(string(slices[0])), slices[1])
            if !used {
                err = errors.New(fmt.Sprintf("Field unused by message NestedParties2_Nested2PartyIDs (%s)", string(field)))
            }
        }
        if err != nil {
            break
        }
    }
    return err
}


func (m *NestedParties2_Nested2PartyIDs) UnmarshalFieldFIX(id int, value []byte) (bool, error) {
    // Check if we are currently populating a group
    used := false
    var err error = nil
    for !used && m._controlBlock.mostRecentRepeatingGroup != nil {
        used = m._controlBlock.mostRecentRepeatingGroup[m._controlBlock.mostRecentRepeatingGroupCounter].PopulateNextFieldById(id, value)
        if !used {
            // This group did not need use the latest field, and has all its mandatory fields, so consider it complete.
            m._controlBlock.mostRecentRepeatingGroupCounter++
            if m._controlBlock.mostRecentRepeatingGroupCounter >= len(m._controlBlock.mostRecentRepeatingGroup)  {
                // we have all the repeated groups we expected.
                m._controlBlock.mostRecentRepeatingGroup = nil
            }
        }
    }
    
    // If one of the groups did not consume the KV, try our own fields 
    if !used {
        switch id {
        
        case 757:
            if !m.Nested2PartyID.HasValue() {
                used = true
                err = m.Nested2PartyID.UnmarshalFIX(value)
                if err != nil {
                    return used, err
                }
            }
        
        
        
        case 758:
            if !m.Nested2PartyIDSource.HasValue() {
                used = true
                err = m.Nested2PartyIDSource.UnmarshalFIX(value)
                if err != nil {
                    return used, err
                }
            }
        
        
        
        case 759:
            if !m.Nested2PartyRole.HasValue() {
                used = true
                err = m.Nested2PartyRole.UnmarshalFIX(value)
                if err != nil {
                    return used, err
                }
            }
        
        
        
        case 806:
            // This counter (NoNested2PartySubIDs) indicates the start of repeated group block 
            count, _ := strconv.Atoi(value)
            m.Nested2PartySubIDs = make([]NstdPtys2SubGrp_Nested2PartySubIDs, count)
            m._controlBlock.mostRecentRepeatingGroup = make([]FixMessage, count)
            for i := 0; i < count; i++ {
                m._controlBlock.mostRecentRepeatingGroup[i] = &m.Nested2PartySubIDs[i]
            }
            m._controlBlock.mostRecentRepeatingGroupCounter = 0
            used = true
        
        
        default:
            used = false
        }
    }
   
    return used, err
}


