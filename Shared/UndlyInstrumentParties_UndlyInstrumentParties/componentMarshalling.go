package UndlyInstrumentParties_UndlyInstrumentParties

import "bufio"
import "strconv"
import "fmt"

// Autogenerated at , do not edit



import UndlyInstrumentPtysSubGrp_UndlyInstrumentPartySubIDs "grgrbll/fix/Shared/UndlyInstrumentPtysSubGrp_UndlyInstrumentPartySubIDs"


func (m *UndlyInstrumentParties_UndlyInstrumentParties) MarshalFIX(res *bufio.Writer) error {
    var err error
    
    if m.UndlyInstrumentPartyID.HasValue() {
        (*res).WriteString("1059=")
        val, err := m.UndlyInstrumentPartyID.MarshalFIX()
        if err != nil {
            return err
        }
        (*res).Write(val)
        (*res).WriteString("\x01")
    }
    if m.UndlyInstrumentPartyIDSource.HasValue() {
        (*res).WriteString("1060=")
        val, err := m.UndlyInstrumentPartyIDSource.MarshalFIX()
        if err != nil {
            return err
        }
        (*res).Write(val)
        (*res).WriteString("\x01")
    }
    if m.UndlyInstrumentPartyRole.HasValue() {
        (*res).WriteString("1061=")
        val, err := m.UndlyInstrumentPartyRole.MarshalFIX()
        if err != nil {
            return err
        }
        (*res).Write(val)
        (*res).WriteString("\x01")
    }
    if len(m.UndlyInstrumentPartySubIDs) > 0 {
    
    (*res).WriteString("1062=")
    (*res).WriteString(strconv.Itoa(len(m.UndlyInstrumentPartySubIDs)))
    (*res).WriteString("\x01")
    for _, g := range m.UndlyInstrumentPartySubIDs {
        err = g.MarshalFIX(res)
    }
    }
    
    return err
}

func (m *UndlyInstrumentParties_UndlyInstrumentParties) UnmarshalFIX(input io.Reader) error {
    var err error
    var field []byte
    for field, err = input.ReadSlice([]byte("\x01")); err == nil {
        slices = bytes.Split(field, []byte("="))
        if len(slices) != 2 {
            // TODO handle data fields
            err = errors.New(fmt.Sprintf("Found field without seperator '=' (%s)", string(field))
        } else {
            var used bool
            used, err m.UnmarshalFieldFIX(strconv.Atoi(string(slices[0])), slices[1])
            if !used {
                err = errors.New(fmt.Sprintf("Field unused by message UndlyInstrumentParties_UndlyInstrumentParties (%s)", string(field)))
            }
        }
        if err != nil {
            break
        }
    }
    return err
}


func (m *UndlyInstrumentParties_UndlyInstrumentParties) UnmarshalFieldFIX(id int, value []byte) (bool, error) {
    // Check if we are currently populating a group
    used := false
    var err error = nil
    for !used && m._controlBlock.mostRecentRepeatingGroup != nil {
        used = m._controlBlock.mostRecentRepeatingGroup[m._controlBlock.mostRecentRepeatingGroupCounter].PopulateNextFieldById(id, value)
        if !used {
            // This group did not need use the latest field, and has all its mandatory fields, so consider it complete.
            m._controlBlock.mostRecentRepeatingGroupCounter++
            if m._controlBlock.mostRecentRepeatingGroupCounter >= len(m._controlBlock.mostRecentRepeatingGroup)  {
                // we have all the repeated groups we expected.
                m._controlBlock.mostRecentRepeatingGroup = nil
            }
        }
    }
    
    // If one of the groups did not consume the KV, try our own fields 
    if !used {
        switch id {
        
        case 1059:
            if !m.UndlyInstrumentPartyID.HasValue() {
                used = true
                err = m.UndlyInstrumentPartyID.UnmarshalFIX(value)
                if err != nil {
                    return used, err
                }
            }
        
        
        
        case 1060:
            if !m.UndlyInstrumentPartyIDSource.HasValue() {
                used = true
                err = m.UndlyInstrumentPartyIDSource.UnmarshalFIX(value)
                if err != nil {
                    return used, err
                }
            }
        
        
        
        case 1061:
            if !m.UndlyInstrumentPartyRole.HasValue() {
                used = true
                err = m.UndlyInstrumentPartyRole.UnmarshalFIX(value)
                if err != nil {
                    return used, err
                }
            }
        
        
        
        case 1062:
            // This counter (NoUndlyInstrumentPartySubIDs) indicates the start of repeated group block 
            count, _ := strconv.Atoi(value)
            m.UndlyInstrumentPartySubIDs = make([]UndlyInstrumentPtysSubGrp_UndlyInstrumentPartySubIDs, count)
            m._controlBlock.mostRecentRepeatingGroup = make([]FixMessage, count)
            for i := 0; i < count; i++ {
                m._controlBlock.mostRecentRepeatingGroup[i] = &m.UndlyInstrumentPartySubIDs[i]
            }
            m._controlBlock.mostRecentRepeatingGroupCounter = 0
            used = true
        
        
        default:
            used = false
        }
    }
   
    return used, err
}


